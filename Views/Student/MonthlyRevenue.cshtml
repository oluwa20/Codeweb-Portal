@model SMS.ViewModels.RevenueViewModel

@{
    ViewData["Title"] = "Monthly Revenue";
}




@{
    Layout = "_LayoutLoggedIn";
}


<div class="px-6 py-5">
    <h1 class="border-b text-3xl font-semibold py-3"> This Month Revenue</h1>

<<<<<<< HEAD



<div class="container mx-auto p-8">
    <h1 class="text-3xl font-bold mb-6">Monthly Revenue Report</h1>

    <div class="bg-white p-6 rounded shadow mb-4">
        <h2 class="text-xl font-semibold mb-2 border-b border-orange">Current Month</h2>
        <div class="border-b border-orange py-1 flex justify-between">
            <p class="">Total Payments for the Month:</p>
            <p>₦@Model.TotalPaymentsForMonth</p>
        </div>
        <div class="flex justify-between pt-2">
            <p>Total Expenditure for the Month:</p>
            <p>₦@Model.TotalExpenditureForMonth</p>
        </div>
    </div>

    <div class="bg-white p-6 rounded shadow mb-4">
        <h2 class="text-xl font-semibold mb-2 border-b border-orange">Current Year</h2>
        <div class="border-b border-orange py-1 flex justify-between">
            <p>Total Payments for the Year:</p>
            <p>₦@Model.TotalPaymentsForYear</p>
        </div>
        <div class="flex justify-between pt-2">
            <p>Total Expenditure for the Year:</p>
            <p>₦@Model.TotalExpenditureForYear</p>
        </div>
    </div>

    <div class="bg-white p-6 rounded shadow mb-4">
        <h2 class="text-xl font-semibold mb-4">All Time</h2>
       <div class="flex justify-between">
            <p>Total Expenditure:</p>
            <p class="text-lg">₦@Model.TotalAllExpenditures</p>
       </div>
    </div>
</div>














@* <div class="container mx-auto mt-8">
=======
    <div class="border-b space-y-4 py-2">
        <p>Total Payment =></p>
        <p>Total Expenditure =></p>
    </div>
    <p class="py-2">Balance For This Week =></p>
</div>
@*
<div class="container mx-auto mt-8">c
>>>>>>> 3316e6a5a4d359ab4310b907110e6a44bff86015
    <h1 class="text-3xl font-semibold mb-4">Monthly Revenue Report</h1>

    <div class="flex mb-4">
        <div class="w-1/2">
            <p><strong>Start Month:</strong> @Model.StartMonth.ToString("MMMM yyyy")</p>
            <p><strong>End Month:</strong> @Model.EndMonth.ToString("MMMM yyyy")</p>
        </div>
        <div class="w-1/2 text-right">
            <p><strong>Total Revenue:</strong> ₦@Model.TotalRevenue.ToString("0.00")</p>
            <p><strong>Total Expenditures:</strong> ₦@Model.TotalAllExpenditures.ToString("0.00")</p>
        </div>
    </div>

    <h2 class="text-xl font-semibold mb-2">Monthly Revenue Details</h2>

    <table class="min-w-full border rounded-lg overflow-hidden">
        <thead class="bg-gray-100">
            <tr>
                <th class="w-1/2 px-4 py-2">Description</th>
                <th class="w-1/4 px-4 py-2">Amount (₦)</th>
                <th class="w-1/4 px-4 py-2">Date</th>
            </tr>
        </thead>
<<<<<<< HEAD
        <tbody> *@
            <!-- Loop through your revenue items and display them in rows -->
            <!-- Example:
          @*   @foreach (var revenueItem in Model.RevenueItems)
            {
                <tr>
                    <td class="border px-4 py-2">@revenueItem.Description</td>
                    <td class="border px-4 py-2">₦@revenueItem.Amount.ToString("0.00")</td>
                    <td class="border px-4 py-2">@revenueItem.Date.ToString("yyyy-MM-dd")</td>
                </tr>
            } *@
            -->
        @* </tbody>
    </table>
</div>
 *@
=======
        <tbody>
           
        </tbody>
    </table>
</div>*@

>>>>>>> 3316e6a5a4d359ab4310b907110e6a44bff86015





































@* <form method="post" asp-action="MonthlyRevenue">
    <div class="form-group">
        <label for="startMonth">Start Date:</label>
        <input type="date" class="form-control" id="startMonth" name="startMonth" required />
    </div>

    <div class="form-group">
        <label for="endMonth">End Date:</label>
        <input type="date" class="form-control" id="endMonth" name="endMonth" required />
    </div>

    <button type="submit" class="btn btn-primary">Calculate Revenue</button>
</form>

@if (Model != null)
{
    <div class="alert alert-success mt-3">
        Total Revenue: ₦@Model.TotalRevenue
    </div>
}
else
{
    <div class="alert alert-warning mt-3">
        No revenue found for the specified period.
    </div>
} *@






@* <section class="mt-10 md:flex sm:px-6 md:px-9 md:space-x-8 md:pt-16 md:justify-evenly md:items-center lg:justify-start lg:px-20">
    <nav class="px-3 space-y-3 lg:w-1/2">
        <p class="text-xl font-bold md:text-2xl lg:text-3xl lg:pb-5">Monthly Payments</p>
        <form method="post" action="" class="border border-grey shadow-lg px-3 py-5 space-y-2 rounded-xs md:px-10 md:py-7 md:space-y-4">
            <div class="space-y-1 lg:space-y-2">
                <label class="text-black font-semibold text-[16px] md:text-xl">Start Date:</label>
                <input id="endMonth" name="endMonth" required type="date" class="border border-grey w-full px-3 py-1 placeholder:text-primary focus:outline-none
                        focus:border-secondary md:w-full">
            </div>
            <div class="space-y-1 lg:space-y-2">
                <label class="text-black font-semibold text-[16px] md:text-xl">End Date:</label>
                <input id="endMonth" name="endMonth" required type="date" class="border border-grey w-full px-3 py-1 focus:outline-none focus:border-secondary">
            </div>
            <div class="">
                <button class="bg-orange-600 text-white rounded-lg text-center font-bold w-full py-2 mt-4" style="color:whitesmoke; ">Submit</button>
            </div>
        </form>
    </nav>
    @if (Model != null)
    {
        <div class="alert alert-success mt-3" style="font-size:inherit.5em;">
            Total Revenue: ₦@Model.TotalRevenue
        </div>
    }
    else
    {
        <p class="text-orange">No payments found for the specified period.</p>
    }
</section> *@































<!-- ... (your HTML head and styles) ... -->

@* <body class="bg-gray-100 font-sans">
  
    <div class="flex flex-col space-y-20">
  
        @using (Html.BeginForm("MonthlyRevenue", "Student", FormMethod.Post, new { @class = "max-w-xs mx-auto space-y-4" }))
        {
            <div class="mb-4">
                @Html.LabelFor(model => model.StartMonth, new { @class = "block text-gray-600 text-sm font-semibold" })
                @Html.EditorFor(model => model.StartMonth, new { htmlAttributes = new { @class = "w-full border border-gray-300 rounded-md py-2 px-3 focus:outline-none focus:border-orange-500" } })
            </div>

            <div class="mb-4">
                @Html.LabelFor(model => model.EndMonth, new { @class = "block text-gray-600 text-sm font-semibold" })
                @Html.EditorFor(model => model.EndMonth, new { htmlAttributes = new { @class = "w-full border border-gray-300 rounded-md py-2 px-3 focus:outline-none focus:border-orange-500" } })
            </div>

            <button type="submit" class="bg-orange-500 text-white font-semibold py-2 px-4 rounded hover:bg-orange-600 focus:outline-none focus:ring focus:border-orange-400">
                Calculate Revenue
            </button>
        }
      
        @if (Model != null )
        {
            <h2 class="text-2xl font-semibold text-orange-500">Total Revenue: &#8358;@Model.TotalRevenue</h2>
        }
    
    </div>

   
</body>

</html>


</html> *@

