@model SMS.ViewModels.RevenueViewModel

@{
    ViewData["Title"] = "Monthly Revenue";
}




@{
    Layout = "_LayoutLoggedIn";
}
@* <form method="post" asp-action="MonthlyRevenue">
    <div class="form-group">
        <label for="startMonth">Start Date:</label>
        <input type="date" class="form-control" id="startMonth" name="startMonth" required />
    </div>

    <div class="form-group">
        <label for="endMonth">End Date:</label>
        <input type="date" class="form-control" id="endMonth" name="endMonth" required />
    </div>

    <button type="submit" class="btn btn-primary">Calculate Revenue</button>
</form>

@if (Model != null)
{
    <div class="alert alert-success mt-3">
        Total Revenue: ₦@Model.TotalRevenue
    </div>
}
else
{
    <div class="alert alert-warning mt-3">
        No revenue found for the specified period.
    </div>
} *@





<!-- ... (your HTML head and styles) ... -->

<body class="bg-gray-100 font-sans">
    <!-- ... (your sidebar and navbar) ... -->
    <!-- BODY OF THE PAGE -->
    <div class="flex flex-col space-y-20">
        <!-- FORM FOR DATE RANGE -->
        @using (Html.BeginForm("MonthlyRevenue", "Revenue", FormMethod.Post, new { @class = "max-w-xs mx-auto space-y-4" }))
        {
            <div class="mb-4">
                @Html.LabelFor(model => model.StartMonth, new { @class = "block text-gray-600 text-sm font-semibold" })
                @Html.EditorFor(model => model.StartMonth, new { htmlAttributes = new { @class = "w-full border border-gray-300 rounded-md py-2 px-3 focus:outline-none focus:border-orange-500" } })
            </div>

            <div class="mb-4">
                @Html.LabelFor(model => model.EndMonth, new { @class = "block text-gray-600 text-sm font-semibold" })
                @Html.EditorFor(model => model.EndMonth, new { htmlAttributes = new { @class = "w-full border border-gray-300 rounded-md py-2 px-3 focus:outline-none focus:border-orange-500" } })
            </div>

            <button type="submit" class="bg-orange-500 text-white font-semibold py-2 px-4 rounded hover:bg-orange-600 focus:outline-none focus:ring focus:border-orange-400">
                Calculate Revenue
            </button>
        }
        <!-- FORM ENDS -->
        <!-- DISPLAY CALCULATED REVENUE -->
        @if (Model != null )
        {
            <h2 class="text-2xl font-semibold text-orange-500">Total Revenue: &#8358;@Model.TotalRevenue</h2>
        }
        <!-- DISPLAY REVENUE ENDS -->
        <!-- ... (rest of your page content) ... -->
    </div>

    <!-- ... (your scripts) ... -->
</body>

</html>


</html>

